apiVersion: v1
kind: Secret
metadata:
  name: mtls-ca
  namespace: qiflowai
  annotations:
    purpose: "Ingress client auth CA bundle"
type: Opaque
data:
  # 使用 base64 编码的CA证书内容（PEM），例如：cat ca.crt | base64 -w0
  ca.crt: REPLACE_WITH_BASE64_CA_PEM
