apiVersion: v1
kind: Secret
metadata:
  name: mtls-ca-issuer
  namespace: qiflowai
  annotations:
    purpose: "cert-manager CA issuer keypair"
type: kubernetes.io/tls
data:
  # base64 编码的 CA 私钥与证书（注意：强权限保护）
  tls.key: REPLACE_WITH_BASE64_CA_KEY
  tls.crt: REPLACE_WITH_BASE64_CA_CERT
