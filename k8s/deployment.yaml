apiVersion: apps/v1
kind: Deployment
metadata:
  name: qiflowai-app
  namespace: qiflowai
  labels:
    app: qiflowai
    component: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: qiflowai
      component: frontend
  template:
    metadata:
      labels:
        app: qiflowai
        component: frontend
    spec:
      containers:
      - name: app
        image: your-registry/qiflowai:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: qiflowai-secrets
              key: database-url
        - name: NEXTAUTH_SECRET
          valueFrom:
            secretKeyRef:
              name: qiflowai-secrets
              key: nextauth-secret
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: qiflowai-secrets
              key: redis-url
        envFrom:
        - configMapRef:
            name: qiflowai-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/ready
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
        volumeMounts:
        - name: uploads
          mountPath: /app/uploads
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: uploads
        persistentVolumeClaim:
          claimName: qiflowai-uploads-pvc
      - name: logs
        persistentVolumeClaim:
          claimName: qiflowai-logs-pvc
      imagePullSecrets:
      - name: registry-secret

---
apiVersion: v1
kind: Service
metadata:
  name: qiflowai-service
  namespace: qiflowai
spec:
  selector:
    app: qiflowai
    component: frontend
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qiflowai-ingress
  namespace: qiflowai
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/auth-tls-secret: "qiflowai/mtls-ca"
    nginx.ingress.kubernetes.io/auth-tls-verify-client: "on"
    nginx.ingress.kubernetes.io/auth-tls-verify-depth: "2"
    nginx.ingress.kubernetes.io/auth-tls-pass-certificate-to-upstream: "true"
    nginx.ingress.kubernetes.io/auth-url: "http://mtls-authz.qiflowai.svc.cluster.local:8080/authorize"
    nginx.ingress.kubernetes.io/auth-response-headers: "X-Authz-User, X-Authz-Serial"
    nginx.ingress.kubernetes.io/auth-snippet: |
      proxy_set_header X-SSL-Client-Serial $ssl_client_serial;
      proxy_set_header X-SSL-Client-Verify $ssl_client_verify;
      proxy_set_header X-SSL-Client-S-DN $ssl_client_s_dn;
spec:
  tls:
  - hosts:
    - your-domain.com
    - www.your-domain.com
    secretName: qiflowai-tls
  rules:
  - host: your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: qiflowai-service
            port:
              number: 80
  - host: www.your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: qiflowai-service
            port:
              number: 80

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qiflowai-uploads-pvc
  namespace: qiflowai
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qiflowai-logs-pvc
  namespace: qiflowai
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: qiflowai-config
  namespace: qiflowai
data:
  APP_URL: "https://your-domain.com"
  NEXTAUTH_URL: "https://your-domain.com"
  SMTP_HOST: "smtp.example.com"
  SMTP_PORT: "587"
  EMAIL_FROM: "QiFlow AI <noreply@example.com>"
  GROWTH_ENABLED: "true"
  REFERRAL_DEFAULT_REWARD: "50"
  FRAUD_DETECTION_ENABLED: "true"
  RATE_LIMIT_ENABLED: "true"

---
apiVersion: v1
kind: Secret
metadata:
  name: qiflowai-secrets
  namespace: qiflowai
type: Opaque
stringData:
  database-url: "postgresql://user:pass@postgres:5432/qiflowai"
  nextauth-secret: "your-nextauth-secret"
  redis-url: "redis://:password@redis:6379"
  jwt-secret: "your-jwt-secret"
  smtp-pass: "your-smtp-password"

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: qiflowai-hpa
  namespace: qiflowai
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: qiflowai-app
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80